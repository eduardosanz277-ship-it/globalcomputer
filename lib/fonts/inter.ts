import { Inter } from "next/font/google";

/** Inter para bloques de marketing / tienda (misma config que páginas de catálogo). */
export const inter = Inter({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
  display: "swap",
});
